Run the suites from within this directory by importing `api.json` and `tests.postman_collection.json` into Postman and selecting either `environment.json` (default `localhost:8089`) or `tests.postman_environment.json`; the test suite requires the server to already contain a `reference_sources` directory under its sources root because it copies that tree into `sources/{{test_sources}}` during setup.